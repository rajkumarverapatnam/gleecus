export interface graphDataType {
    CLINICIAN_NAME: string;
    counts: number;
}

export interface tableDataType {
    createdAt: Date;
    name: string;
    avatar: string;
    email: string;
    phone: string;
    id: number;
}